services:
  - type: web
    name: koshagar-backend
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: KOSHAGAR_DATA_DIR
        value: /tmp/koshagar
